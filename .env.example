# SWS environment template. Copy to .env for local development only.
# Never commit a real .env or real credentials.

# AWS connection identity (used by future AWS integrations only).
# The hermetic unit-test suite does not require these.
# AWS_REGION=us-east-1
# AWS_PROFILE=my-profile